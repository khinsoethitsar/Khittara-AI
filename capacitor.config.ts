import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khittara.ai',
  appName: 'ခေတ္တရာ Ai',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
